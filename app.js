//All of my required files
var inquirer = require('inquirer');

var basicCard = require('basic_card');

var clozeCard = require('cloze_card');

inquirer.prompt([]).then(function (answers) {
    // Use user feedback for... whatever!!
});

